{{ if .Values.framework }}
{{ if eq .Values.framework "operator-sdk" }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
    name: {{ include "registry-regulation-management.serviceAccountName" . }}-edp
roleRef:
    kind: Role
    name: edp-{{ include "registry-regulation-management.serviceAccountName" . }}-role
    apiGroup: rbac.authorization.k8s.io
subjects:
    - kind: ServiceAccount
      name: {{ include "registry-regulation-management.serviceAccountName" . }}
{{ end }}
{{ end }}